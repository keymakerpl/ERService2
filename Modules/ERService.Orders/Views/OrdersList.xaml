<UserControl
    x:Class="ERService.Orders.Views.OrdersList"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="clr-namespace:ERService.Orders.Views"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:prism="http://prismlibrary.com/"
    xmlns:syncfusion="http://schemas.syncfusion.com/wpf"
    prism:ViewModelLocator.AutoWireViewModel="True"
    mc:Ignorable="d">

    <UserControl.Resources>
        <Style x:Key="customCellStyle" TargetType="syncfusion:GridCell">
            <Setter Property="CellSelectionBrush" Value="Transparent" />
        </Style>
    </UserControl.Resources>

    <Grid>
        <syncfusion:SfDataGrid
            x:Name="dataGrid"
            AllowFiltering="True"
            CellStyle="{StaticResource ResourceKey=customCellStyle}"
            ItemsSource="{Binding Orders, UpdateSourceTrigger=PropertyChanged}"
            SelectionMode="Single" />
    </Grid>
</UserControl>
